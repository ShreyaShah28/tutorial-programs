//Random numbers
console.log( Math.floor(Math.random() * max ) + min);
// Here max means the maximum number that could be derived 

//To derive random numbers in between a fixed range
console.log( Math.floor(Math.random() * (max-min) ) + min);